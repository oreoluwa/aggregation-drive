const boxController = require('./box')
const dropboxController = require('./dropbox');
const googleDriveController = require('./googleDrive');

module.exports = {
  boxController,
  dropboxController,
  googleDriveController,
}
